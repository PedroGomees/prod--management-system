<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <style>
        #relatorio-main {
            font-family: Arial, sans-serif;
            background-color: #1a2835;
            height: auto;
            margin: 0 auto;
            padding: 0;
            max-width: 1000px;
            margin-top: 350px;
            display: block;
            padding: 20px;
            background-color: #c0e0ff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        #relatorio-title {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        #relatorio-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

        #relatorio-table th, #relatorio-table td {
            padding: 15px; /* Reduzido para dar mais espaço */
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        #relatorio-table th {
            background-color: #007BFF;
            color: white;
        }

        #relatorio-table tr:hover {
            background-color: #f1f1f1;
        }

        #relatorio-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            padding: 10px;
            background-color: #007BFF;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }

        #relatorio-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <main id="relatorio-main">
        <h1 id="relatorio-title">{{titulo}}</h1>
        <table id="relatorio-table">
            <thead>
                <tr>
                    <th>Nome do Produto</th>
                    <th>Total Quantidade</th>
                </tr>
            </thead>
            <tbody>
                {{#each produtos}}
                    <tr>
                        <td>{{this.nome_produto}}</td>
                        <td>{{this.total_quantidade}}</td>
                    </tr>
                {{else}}
                    <tr>
                        <td colspan="2">Nenhum produto cadastrado.</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </main>
</body>
</html>
